apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-init
  namespace: group5
data:
  init.sql: |
    CREATE TABLE IF NOT EXISTS participants (
        id INT AUTO_INCREMENT PRIMARY KEY,
        name VARCHAR(100) NOT NULL
    );
